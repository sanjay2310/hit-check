#include <stdio.h>
#include <math.h>
 int main()
  {
	     int  a, b;
             	
		scanf("%d%d",&a,&b);
          
	    printf("fmaxx  = %d\n",fmax (a,b));
	    printf("fmin = %d\n",fmin (a,b));
	 
	return 0;
}

